把服务器实现为整个的大服架构，对外是进行逻辑分服的，对内并不进行分服，
既能够实现运营滚服的要求，也能够实现以后需要全球大服的要求。
对于运营来说，合服的操作应该能够自动化进行
需要有数据唯一服的要求

客户端与服务器的通信暂时使用Google Protobuf。后端机器之间的通信考虑使用RPC的使用

策划配置这块的读取暂时是使用工具自动生成的txt格式，txt格式只是用来对接程序读取的，
程序跟策划沟通还是考虑使用excel的方式更好


Google Protobuf的选择，默认支持的是proto3的，proto2考虑进行兼容